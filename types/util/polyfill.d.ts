/**
 * Utility that provides polyfills for browser compatibility
 * 
 * This includes polyfills for older browsers that might not support
 * newer JavaScript features used by draw2d.
 */
export class Polyfill {
  /**
   * Applies all available polyfills
   * This method is typically called once during draw2d initialization
   */
  static apply(): void;
}