import { Icon } from './icon';

/**
 * A sun icon.
 */
export class Sun extends Icon {
  /**
   * Creates a new sun icon
   * @param {Object} [attr] the configuration of the shape
   */
  constructor(attr?: any, setter?: any, getter?: any);

  /**
   * @inheritdoc
   */
  createSet(): any;
}